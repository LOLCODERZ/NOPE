enum Instruction {
    PushStack = 0x00,
    Add,
    Subtract,
    Multiply,
    Divide,
    Jump,
    JumpEqual,
    JumpNotEqual,
    // Merge,
    // Split,
    // Append,
    // Load,
    Interrupt
};