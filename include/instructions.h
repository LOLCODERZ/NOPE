enum Instruction {
    Push = 0x00,
    Pop,
    Add,
    Subtract,
    Multiply,
    Divide,
    Jump,
    JumpEqual,
    JumpNotEqual,
    // Merge,
    // Split,
    // Append,
    // Load,
    Interrupt
};